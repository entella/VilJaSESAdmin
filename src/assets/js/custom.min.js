function equalH(){
		var window_width = $( window ).width();
		
		$(".equal-boxH").height('auto');	
		var maxHeight = Math.max.apply(null, $(".equal-boxH").map(function (){
			return $(this).height();
		}).get());
		if (window_width > 481) {
			$(".equal-boxH").height(maxHeight);	
		}else{
			$(".equal-boxH").removeAttr("style");
		}
}

$(window).on('resize',function() {
	equalH();
});

$(document).ready(function() {
	equalH();
	
	$('.collapse-mthdata').click(function(){
		$(this).closest(".collapse-mthdata").toggleClass("open-Mdata");
	
		if ($(this).closest(".collapse-mthdata").hasClass("open-Mdata")) {
			$(this).closest("tr").next('.inner-Mdata:eq(0)').show();
			
		}else{
			$(this).closest("tr").next('.inner-Mdata:eq(0)').hide();
		}
	
	});

	$('.open-markRPT').click(function(){
		console.log('jquery block');
		$(this).closest(".collapse-mthdata").toggleClass("open-Mdata");
	
		if ($(this).closest(".collapse-mthdata").hasClass("open-Mdata")) {
			$(this).closest("tr").next('.inner-Mdata:eq(0)').show();
			
		}else{
			$(this).closest("tr").next('.inner-Mdata:eq(0)').hide();
		}
	});
});


